# ConvertGeine PDF Compressor

Dedicated Ghostscript PDF compression service for ConvertGeine.

## Endpoints

- `GET /health`
- `POST /compress`

## Form fields

- `file`: PDF file
- `level`: `maximum`, `balanced`, or `quality`
